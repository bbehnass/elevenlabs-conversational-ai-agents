# Server-side only. Used by app/actions/getSignedUrl.js to request a signed URL.
ELEVEN_LABS_API_KEY=your-elevenlabs-api-key

# Agent ID from https://elevenlabs.io/app/conversational-ai (authentication must be enabled on the agent)
NEXT_PUBLIC_AGENT_ID=your-agent-id
